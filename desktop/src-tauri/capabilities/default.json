{
  "identifier": "daemon-desktop-default",
  "description": "Default capabilities for the Daemon desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-spawn",
    "shell:allow-execute",
    "shell:allow-stdin-write",
    "store:default",
    {
      "identifier": "shell:allow-open",
      "allow": [
        { "path": "https://ollama.com/**" }
      ]
    }
  ]
}
